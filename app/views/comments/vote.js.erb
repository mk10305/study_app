<% if @vote.valid? %>

$("#comment_<%= @comment.id %>_votes").html("<%= @comment.total_votes %> <span class='glyphicon glyphicon-heart'></span>")

<% else %>

alert("You can only vote on a comment once.");

<% end %>