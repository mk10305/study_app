<% if @vote.valid? %>

$("#post_<%= @post.id %>_votes").html("<%= @post.total_votes %> <span class='glyphicon glyphicon-heart'></span>")

<% else %>

alert("You can only vote on a post once.");

<% end %>